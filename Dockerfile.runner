FROM python:3.11-slim

WORKDIR /sandbox

RUN pip install --no-cache-dir \
      fastapi==0.115.0 \
      httpx==0.27.2 \
      pydantic==2.9.2 \
      starlette==0.38.6 \
      uvicorn==0.30.6

RUN useradd -m -u 10001 runner
USER runner

ENV PYTHONUNBUFFERED=1

CMD ["python3", "--version"]