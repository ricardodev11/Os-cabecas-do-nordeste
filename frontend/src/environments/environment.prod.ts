// Ambiente de produção (usado pelo build com fileReplacements — ver angular.json).
// Ajuste `apiBaseUrl` para o endereço real da API antes do deploy.
export const environment = {
  production: true,
  apiBaseUrl: 'http://localhost:8000/api/v1',
  apiOrigin: 'http://localhost:8000',
};